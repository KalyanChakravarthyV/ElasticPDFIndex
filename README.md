# Tool to index PDFs fetched from MongoDB (JSON Object)

`TODO`: Add Text
###To install run
```bash
pip install -e .
```

###Once installed, you can run it via 
```bash
elastic_pdf_index -i <inputfile> 
```